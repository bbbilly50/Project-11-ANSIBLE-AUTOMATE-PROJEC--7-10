- name: uninstall apache from uat-webservers
  hosts: uat-webservers
  remote_user: ec2-user
  become: yes
  become_user: root
  tasks:
  - name: delete httpd
    yum:
      name: httpd
        state: removed